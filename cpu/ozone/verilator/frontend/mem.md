// Dispatch will make an ROB entry 
Stores -> are going to come with the value. 
// Dispatch (has to handle what values it sends to the memory system. )
Stores and loads come in with their ROB ID. 

we get the 